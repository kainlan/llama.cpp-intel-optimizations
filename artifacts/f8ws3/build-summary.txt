impl-f8ws3 build + gate record, 2026-08-15 01:30
Worktree /Apps/llama.cpp-hm-f8ws, branch task/hm-f8ws, HEAD 1c548e15b, tree clean.

COMMITS COVERED
  76041c5a5 00:46:00  fix(sycl): release buffer-owned entries through their own owner
  b94fb1373 00:56:49  fix(sycl): publish allocation-owned experts for the prompt admission too
  1c548e15b 01:02:08  test(sycl): gate that both MUL_MAT_ID admissions publish before resolving

BUILD
  build1  start 00:59:21  end 01:14:22  BUILD_RC=0  0 error/FAILED lines  (-j 8)
  build2  BUILD_RC=0  0 error/FAILED lines
  build2 was NOT redundant: committing tests/CMakeLists.txt at 01:02 postdated build1's
  CMake regen, so build1's `ninja -n` still reported "[0/1] Re-running CMake" -- work to
  do, i.e. NOT current.  build2 absorbed the regen (36 steps, full ggml-sycl.cpp rebuild).

  CURRENCY, not just freshness:
    ninja -n ggml-sycl test-sycl-reset-model-weight-lease-preserve test-backend-ops \
            llama-completion llama-cli llama-bench   ->   "ninja: no work to do."
  Artifacts: libggml-sycl.so.0.15.3 01:30:26, all binaries 01:30:2x -- all postdate all
  three commits.

NOT A CPU-FALLBACK BUILD
  GGML_SYCL:BOOL=ON
  ldd | grep -cE 'libggml-sycl|libsycl'  = 2 for test-backend-ops AND llama-completion

ARTIFACT PROBES -- both halves scored, presence control BEFORE absence
  RED column captured from the 00:57:56 .so, which contains 76041c5a5 but not b94fb1373,
  i.e. the immediately-prior artifact.  nm matched on the substring WITH the open paren,
  never grep -w (mangled-name trap).

  probe                                                        RED   GREEN
  strings "[MOE-DECODE-CANONICAL] tensor=%s published=..."       1      1   <- control
  strings "[MOE-PROMPT-CANONICAL] tensor=%s published=..."       0      1   <- b94fb1373
  nm -C   "ggml_sycl_publish_backend_aos_expert_handles("        1      1   <- control
  nm -C   "ggml_sycl_publish_mmid_canonical_aos_experts("        0      1   <- b94fb1373

  Both controls fired in the RED column, so the two zeros there were measurements rather
  than unmatchable patterns.  The nm target was at risk of proving nothing (static, two
  call sites, could have been inlined away) -- it survived, but the string probe was the
  load-bearing one and was treated as such.

  76041c5a5 adds NO new string or symbol -- it is a call swap plus comments.  Its artifact
  evidence is the currency check above plus its source gate, and nothing more is claimed.

HOST-SAFE GATES (no GPU, no model load)
  Registration selects a non-empty set first, so the run cannot pass vacuously:
    ctest -N -R 'mmid-publish-parity'   ->   Test #358, "Total Tests: 1"

    #355 test-sycl-universal-provenance-source ......... Passed  3.91 sec
    #356 test-sycl-moe-refusal-reason-source .......... Passed  2.13 sec
    #357 test-sycl-moe-prestage-block-guard-source .... Passed  2.88 sec
    #358 test-sycl-mmid-publish-parity-source ......... Passed  1.40 sec
    100% tests passed, 0 tests failed out of 4

  The new gate carries 2 methodology controls ordered FIRST (both regions exist; regions
  disjoint) and 5 witnessed mutations applied inside the region they target.  Its mutation
  control failed on its first run and the GATE was wrong, not the source: allow_materialize
  is an ARGUMENT, so a region ending at the resolver call's name excluded it and that check
  would have passed on any tree at all.  Region now runs through the closing paren.

NOT RUN BY ME (lead-only, per house standard)
  - sycl-reset-model-weight-lease-preserve (GPU; ctest name has NO test- prefix, so
    `-R '^test-sycl-reset-model-weight-lease-preserve$'` selects zero and passes
    vacuously -- verify with -N first).  This is the gate that decides whether the
    :9457 abort is closed and all 9 cases pass.  It is built and registered here.
  - census 6, test-backend-ops, any model-loading gate.

================================================================================
CORRECTION, 01:40 -- prompted by impl-f8ws2's cross-commit-contamination warning
================================================================================

f8ws2 warned (relayed by the lead) that build/bin was not attributable to any
single commit: build5 ran 00:46->00:57:59 while b94fb1373 landed at 00:56:49,
mid-flight.  The warning is WELL-FOUNDED and I am recording it rather than
waving it off, because it came within 8 seconds of biting (below).  It does not
invalidate anything handed over, for reasons that are checkable:

1. THE RED BASELINE IS UNAFFECTED.  build5 never recompiled ggml-sycl.cpp --
   `grep -c "ggml-sycl.cpp.o" build5.log` = 0.  Every probe in the table above
   targets that TU, so the object behind the RED column was frozen at the 00:38
   build, which predates b94fb1373 entirely.  The first RED capture (from the
   00:38 .so directly) and the second (from the 00:57:56 .so) agree on all four
   probes, which is what two independent pre-change artifacts should do.

2. THE GREEN ARTIFACTS ARE ATTRIBUTABLE AT OBJECT LEVEL.  Not inferred from the
   link line -- read off the objects:

     76041c5a5 committed 00:46:00  ->  unified-cache.cpp.o  00:46:38
     b94fb1373 committed 00:56:49  ->  ggml-sycl.cpp.o      01:02:00
     1c548e15b committed 01:02:08  ->  tests/ ONLY, zero ggml/ or src/ files
                                       libggml-sycl.so      01:30:26

   Each object postdates its own commit; the .so postdates every object.

   THE 8-SECOND NEAR-MISS: ggml-sycl.cpp.o finished at 01:02:00 and 1c548e15b
   landed at 01:02:08.  Had that commit touched ggml-sycl.cpp, the object would
   have been exactly the ambiguous artifact f8ws2 is warning about.  It touched
   only tests/CMakeLists.txt and one .py, so the TU is unambiguously b94fb1373's
   source -- but the margin was eight seconds, not a design.

3. THE DECISIVE TEST, and `ninja -n` IS THE WRONG INSTRUMENT FOR IT HERE.

     find ggml src common -type f \( -name '*.cpp' -o -name '*.hpp' \
          -o -name '*.h' -o -name '*.c' \) -newer build/bin/libggml-sycl.so.0.15.3
     -> 0 files

   Zero sources postdate the shipped .so.  That is the currency claim, and it
   holds at the current HEAD.

   ⚠️ `ninja -n` DOES NOT SETTLE CURRENCY IN THIS WORKTREE, and the summary above
   leaned on it.  CMake's configure step depends on the git index:

     ninja explain: output build.ninja older than most recent input
                    /Apps/llama.cpp/.git/worktrees/llama.cpp-hm-f8ws/index

   So ANY commit -- including the docs-only 96b92f074, which touched nothing but
   artifacts/*.txt -- makes `ninja -n` print "[0/1] Re-running CMake" forever
   after.  "ninja: no work to do" was true when measured (immediately after
   build2, at HEAD 1c548e15b, before the docs commit) and stopped being true the
   moment a commit of ANY kind landed.  It is a statement about the index
   timestamp, not about whether the artifacts match the sources.

   Worse, the regen is not free: it rewrites build.ninja and cascades into a
   ~36-step relink (that is what build2 was).  So chasing "no work to do" costs a
   full relink and buys nothing.  Use the `-newer` test above instead.
